version: '3.8'
services:
  web:
    build: .
    container_name: poker-jack-game-container
    command: npm start
    volumes:
      - .:/app
    ports:
      - '3050:3050'
    # networks:
    #   - node-network
    # depends_on:
    #   - mongodb
    #   - redis

  # mongodb:
  #   image: mongodb/mongodb-community-server
  #   container_name: mongodb-1
  #   ports:
  #     - '27018:27017' # local port: container port
  #   volumes:
  #     - poker-game-db:/data/db
  #   networks:
  #     - node-network

  # redis:
  #   image: redis:latest
  #   container_name: redis
  #   ports:
  #     - '6381:6379' # local port: container port
  #   networks:
  #     - node-network
# volumes:
#   poker-game-db:

# networks:
#   node-network:
#     driver: bridge
